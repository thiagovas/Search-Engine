#include "parser.hpp"
using namespace std;

// It returns the urls contained on the html.
// The URL's returned are on the canonical form.
vector<string> Parser::GetLinks(string html)
{
  return vector<string>();
}
